## 오늘 할일
- uninit / anon initalizer 연결 경로
- uninit_new 구현/검토 O
- uninit_initialize 흐름 O
- anon_initializer 구현 / 검토 O
- vm_alloc_page_with_initializer에서 type 별 initializer 연결 확인 O
- page fault 시 uninit page가 anon page로 전환되는 흐름 점검 O

## 작업 목록
vm_alloc_page_with_initializer 내부에서 uninit_new 를 완성시킨다
VM_UNINIT = 0,
VM_ANON = 1,
VM_FILE = 2,

1. spt에 유저 va가 없다면, page 를 생성하고, type을 확인한다.
2. type을 확인할 때는 매크로 함수인 TYPE을 사용한다.
3. page_operations
4. uninit_new 생성
5. unitit_new -> 생성한 페이지, va, init, type, aux, type에 따른 initializer
6. 받아온 인자중에 아직 전달하지 않은 writable를 page에 선언
7. page에 선언한 writable을 할당

- uninit_initialize 흐름
1. 접근하기 편하기 위해  새로운 구조체 uninit_page 선언하고, paga ->uninit으로 주소값 넘김
2. page_initializer가 init과 aux값을 바꿀 수 있으므로 따로 저장
3. uninit 에 있는 page_initializer를 실행 
4. init 이 있으면 init 실행
5. init 내부를 채워줘야함.

-uninit 
operations -> &uninit_ops
union -> uninit

struct anon_page 구조체에는 뭐가 있어야 할까?