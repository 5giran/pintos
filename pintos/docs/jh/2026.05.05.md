## 구현 목록
- 현재 작성된 코드 흐름 살펴보기.
- 작성된 코드 흐름 도식화 하기.

1. 부팅 후 테스트 실행 진입 -> 2. 최초 유저 프로세스 생성 -> 3. 스레드 생성과 초기화 4. ->exec와 argument passing -> 5. ELF 로딩 + ROX -> 6. 유저 프로그램에서 시스템콜 발생 7. 현재 syscall 구현 상태 ->
8. exit / wait / child_status

커널이 run 'ls -l foo' 전체 명령어를 받고, 쪼개어 argv배열에 넣음
run_task는 해당 배열을 받고 첫 번째 전체 부분을 process_create_initd(task)에 넣어줌

run_task() ->

process_wait(process_create_initd(task))->

process_create_initd() -> 전체 command line을 받고 sema_init부터 호출
command line을 복사하고, 해당 command line에서 thread 이름만 추출하여 
thread_create 호출 -> tid를 반환 -> process_wait(tid) 호출

process_wait() -> find_child_status로 현재 스레드를 가지고 와서  status를 가지고 오고,
해당 status-> id와 child_tid가 같다면, status를 반환한다.

thread_create() ->initd(f_name)호출 하고 initd() 내부에서 process_exec() 호출

process_exec -> parse_command_line 호출 -> load -> setup_stack -> do_iret

load() -> file_open ->file_read -> file_deny_write



do_iret -> intr_frame에 저장된 rip/rsp/rdi/rsi 등을 CPU 레지스터로 복원 ->
iretq 실행 -> 유저 모드 전환 -> 유저 프로그램 시작

user 쪽에서 syscall을 하면, syscall_handler()가 호출이 되고 syscall_handler -> init_syscall_entry
로 유저 쪽에서 레지스터에 보낸 값을 읽어와서 저장 하고 dispatch_syscall로 들어온 syscall 

- user memory 검증
is_valid_user_buffer -> buf포인터와 size를 받아서 buf_end에 현재 buf포인터와 size로 buf의 끝나는 부분을 구함-> size 길이 유효성 검사, 페이지가 buf_end와 비교하면서 유효 할 때까지 반복 -> get_next_page_if_valid로 해당 ptr이 null 인지, 커널 영역 위인지, pml4 가상 주소에 실제로 해당 부분이 위치하는지?

is_valid_user_string -> str포인터 받음

handle_fork()
