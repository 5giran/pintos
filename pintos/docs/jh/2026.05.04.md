## 구현 할 목록
- process.c -> pallock_free 부분 stack 넣기 전에 free해버려서 fage fault 버그 수정.

-test list
args-single
args-multiple
args-many
args-dbl-space

write(fd=1)

1. putbuf() 위치 확인
2. syscall.c 상단에 include 추가
3. handle_write() 에서 syscall 인자 3개 꺼내기 -> fd, buffer,size
4. put_buf를 활용하여 출력할 buffer,size를 넣고 호출 한다음 어떤 사이즈 만큼 출력했는지
return_value에 size값을 할당



## 질문 목록
1. 주소 변환 과정이 왜 4단계인가? 
-> 64비트 가상 주소 공간이 너무 크기 때문에, 거대한 페이지 테이블 하나를 만들면 메모리 낭비가 극심하다. 그래서 페이지 테이블 자체를 여러 단계의 트리 구조로 나누어 필요한 부분만 만든다.

1-1 가능한 페이지 개수에서  엔트리 하나에 8바이트 하면 한 단게짜리 페이지 테이블 크기가 512GB가 필요하다는 계산이 나오는데 이 부분 아예 모르겠음 왜 말이 안된다는건지 

1-2 9비트씩 처리한다고 하는데 이 부분도 이해가 안감 인덱스 9비트 처리가 뭘 뜻하는지?


2. 각 단계는 왜 필요한가?
주소 변환은 트리 탐색과 비슷

2-1 PML4는 최상위 페이지 테이블 47~39번 비트

2-2 PML4 는 비유 하자면 국가 선택

2-3 PDPT 가상 주소의 38~30번 비트

2-4 PDPT 엔트리 하나가 담당하는 범위는 1GB ->
이 가상 주소가 속한 1GB 구간의 Page Directory가 어디 있는가?
PDPT = 도시 선택

2-5 PD 엔트리가 담당하는 범위는 2MB -> 이 가상 주소가 속한 2MB 구간의
Page Table 이 어디 있는가?
PD = 동네 선택

2-6 PT Page Table 4KB

3. 왜 48 이상에는 47의 숫자로 채우는가?
